# Optional frontend override. The assistant falls back to local mock suggestions
# if this backend is unavailable.
VITE_AI_BACKEND_URL=http://127.0.0.1:8000

# Optional placeholder for a future browser error-monitoring SDK.
VITE_ERROR_MONITORING_DSN=
